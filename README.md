### Project

- WorldwidePhotography client.
- The main Goal: builds the open site-crm for photographers who apply own photos for contests.

### Build app

Run `npm run build`.

### Run app

Run `npm run dev` for start server in dev mode.
Run `npm start` for start server in prod mode.

### Tools and librares.

- ReactJS.
- Webpack.
- Docker.
